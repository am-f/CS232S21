1.By searching the function name “strcmp” in the assembly text, what instruction calls strcmp?
	call strcmp

2.Where is the password “secret” stored?
	After the label "password" at the very top of the assembly code. It's stored as ASCII values 0x73,0x65,0x63,0x72,0x65,0x74,0x0.
