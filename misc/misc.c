#include <stdio.h>
int main() {
  int i = 0;
  char ch = 'm';
  char s[4] = "akn";
  double f = 0.4;
  if(i == (i >= 0 ? i : (-i)) && 2*3 == 6) {
    int i = 1;
  }
  else if(s[1] > ch) {
    int i = 2
  }
}